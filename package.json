{
  "name": "al-rafiah-dessert",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --open index-dev.html --host",
    "build": "tsc -p tsconfig.json && vite build && cp dist/index-dev.html dist/index.html",
    "preview": "vite preview",
    "deploy": "npm run build && gh-pages -d dist -b gh-pages --dotfiles"
  },
  "dependencies": {
    "phaser": "^3.90.0"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "typescript": "^5.3.3",
    "vite": "^5.4.0"
  }
}
